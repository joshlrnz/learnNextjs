import { Inter } from 'next/font/google';
import { Lusitana as LusitanaType } from 'next/font/google';
export const Lusitana = LusitanaType({ subsets: ['latin'], weight: ['400', '700'] });
export const inter = Inter({ subsets: ['latin'] });